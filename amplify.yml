version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'npm install -g pnpm'
                - 'pnpm install'
        build:
            commands:
                - 'pnpm build'
    artifacts:
        baseDirectory: .amplify-hosting
        files:
            - '**/*'
